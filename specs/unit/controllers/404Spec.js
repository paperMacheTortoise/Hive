describe("404Controller", function() {

  beforeEach(angular.mock.module('bizGramApp'));

  it('should have a 404controller', function() {
    expect(App.404Ctrl).not.to.equal(null);
  });

  it('should have a properly working 404 controller', inject(function($rootScope, $state, $stateParams) {

  })

});
