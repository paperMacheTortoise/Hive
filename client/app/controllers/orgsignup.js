angular.module('orgsignupCtrl', ['firebase'])

  .controller('OrgsignupController', function () {
    var vm = this;

    vm.signuporg = function (orgname) {
      // orgsignup factory to get the firebase array
    }
  })
