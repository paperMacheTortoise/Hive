angular.module('inviteFactory', ['firebase'])

.factory('invite', ['$firebaseArray', function ($firebaseArray) {

  var inviteFactory = {};

  inviteFactory.sendEmailInvitation = function () {

  };

  return orgsignupFactory;

}]);
